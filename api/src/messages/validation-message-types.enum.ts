

/**
 * Enum com os tipos de validações.
 *
 * @enum {number}
 */
enum ValidationMessagesTypes {
    PATTERN = "pattern",
    REQUIRED = "required",
    MINLENGTH = "minlength"
};

export default ValidationMessagesTypes;